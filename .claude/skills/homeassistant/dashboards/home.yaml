title: Home
views:
  # ═══════════════════════════════════════════════════════════════════════════
  # ÜBERSICHT - Hauptseite
  # ═══════════════════════════════════════════════════════════════════════════
  - title: Übersicht
    path: home
    icon: mdi:home
    type: sections
    max_columns: 3
    cards: []
    sections:
      # ─────────────────────────────────────────────────────────────────────────
      # Willkommen & Anwesenheit
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        cards:
          - type: markdown
            content: |
              # Willkommen zu Hause 👋
              {% set hour = now().hour %}
              {% if hour < 6 %}Gute Nacht
              {% elif hour < 12 %}Guten Morgen
              {% elif hour < 18 %}Guten Tag
              {% else %}Guten Abend{% endif %}, Philipp!
            card_mod:
              style: |
                ha-card {
                  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                  color: white;
                  border-radius: 16px;
                }

          - type: entity
            entity: weather.forecast_home
            name: Wetter
            icon: mdi:weather-partly-cloudy

      # ─────────────────────────────────────────────────────────────────────────
      # Personen
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        cards:
          - type: tile
            entity: person.philipp
            name: Philipp
            icon: mdi:account
            color: blue
            show_entity_picture: true
            vertical: false

          - type: tile
            entity: person.sophia
            name: Sophia
            icon: mdi:account
            color: pink
            show_entity_picture: true
            vertical: false

      # ─────────────────────────────────────────────────────────────────────────
      # Wetter & Sonne
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        cards:
          - type: weather-forecast
            entity: weather.forecast_home
            show_current: true
            show_forecast: true
            forecast_type: daily
            name: Wetter

          - type: tile
            entity: sun.sun
            name: Sonne
            icon: mdi:white-balance-sunny
            color: amber

      # ─────────────────────────────────────────────────────────────────────────
      # Sonnenzeiten
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        cards:
          - type: tile
            entity: sensor.sun_next_rising
            name: Sonnenaufgang
            icon: mdi:weather-sunset-up
            color: orange

          - type: tile
            entity: sensor.sun_next_setting
            name: Sonnenuntergang
            icon: mdi:weather-sunset-down
            color: deep-orange

  # ═══════════════════════════════════════════════════════════════════════════
  # GERÄTE - Medien & Technik
  # ═══════════════════════════════════════════════════════════════════════════
  - title: Geräte
    path: devices
    icon: mdi:devices
    type: sections
    max_columns: 2
    cards: []
    sections:
      # ─────────────────────────────────────────────────────────────────────────
      # Medien
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        title: Medien
        cards:
          - type: tile
            entity: media_player.wohnzimmer_tv
            name: Wohnzimmer TV
            icon: mdi:television
            color: cyan
            features:
              - type: media-player-volume

      # ─────────────────────────────────────────────────────────────────────────
      # iPhone
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        title: Philipp's iPhone
        cards:
          - type: tile
            entity: sensor.philipps_iphone_battery_level
            name: Batterie
            icon: mdi:battery
            color: green

          - type: tile
            entity: sensor.philipps_iphone_battery_state
            name: Ladestatus
            icon: mdi:battery-charging

          - type: tile
            entity: device_tracker.philipps_iphone
            name: Standort
            icon: mdi:cellphone-marker

  # ═══════════════════════════════════════════════════════════════════════════
  # SYSTEM - Home Assistant Status
  # ═══════════════════════════════════════════════════════════════════════════
  - title: System
    path: system
    icon: mdi:cog
    type: sections
    max_columns: 2
    cards: []
    sections:
      # ─────────────────────────────────────────────────────────────────────────
      # Updates
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        title: Updates
        cards:
          - type: tile
            entity: update.home_assistant_core_update
            name: Home Assistant Core
            icon: mdi:home-assistant
            color: cyan

          - type: tile
            entity: update.home_assistant_supervisor_update
            name: Supervisor
            icon: mdi:package-variant

          - type: tile
            entity: update.home_assistant_operating_system_update
            name: OS
            icon: mdi:linux

      # ─────────────────────────────────────────────────────────────────────────
      # Backup
      # ─────────────────────────────────────────────────────────────────────────
      - type: grid
        title: Backup
        cards:
          - type: tile
            entity: sensor.backup_backup_manager_state
            name: Backup Status
            icon: mdi:backup-restore
            color: green

  # ═══════════════════════════════════════════════════════════════════════════
  # EINKAUFSLISTE
  # ═══════════════════════════════════════════════════════════════════════════
  - title: Einkaufsliste
    path: shopping
    icon: mdi:cart
    type: sections
    max_columns: 1
    cards: []
    sections:
      - type: grid
        cards:
          - type: todo-list
            entity: todo.einkaufsliste
            title: Einkaufsliste
